# NDDev MiMo Code Builder Setup

This target is managed by nddev-mimocode-app. Keep MiMo Code runtime state,
configuration, and authentication inside the explicit target selected by the
manager.

Use the native nddev-builder skill and agent for setup artifact work. Do not
read live user MiMo Code state or provider credentials outside the target.
